header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All Tours
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    //- Chapter 190
    //-This user comes from authController.js > isLoggedIn (basically if the user is logged in,
    //- the isLoggedIn fn pass a user variable(req.locals.user) to the templates, else no user
    //- var.) 
    
    if user
      a.nav__el.nav__el--logout Logout
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Login
      a.nav__el.nav__el--cta(href='#') Sign Up